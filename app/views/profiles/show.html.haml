%h2.page-title Profile
.profile_user
  .profile_user_image
    = image_tag current_user.avatar_image
  .profile_user_basicInfo
    .profile_user_name
      .profile_user_displayName
        #{current_user.display_name} (#{I18n.t("enum.genders.#{current_user.display_gender}")})
      .profile_user_action
        = link_to 'Edit', edit_profile_path
    .profile_user_introduction
      %p= current_user.display_introduction

- current_user.boards.each do |board|
  = link_to board_path(id: board.id) do
    = render partial: 'commons/board', locals: { board: board }